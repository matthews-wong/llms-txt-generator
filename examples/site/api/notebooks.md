# Notebooks API

Create, list, and delete notebooks through the `/v1/notebooks` resource.

## Endpoints

`GET /v1/notebooks` returns every notebook the token can see.

## Rate limits

The API allows 600 requests per minute per token.
